<<<<<<< HEAD
# Learniverse Talk — Vite + React + TypeScript

## Project info

**Lovable project URL**: https://lovable.dev/projects/5a7cf82c-cf08-4246-9a73-96bc8d231281

## Quick start (local)

Requirements: Node 18+ (or a supported version), npm (or pnpm/yarn).

From the project root (PowerShell):

```powershell
# move into the project folder
Set-Location -Path "C:\Users\user\Downloads\learniverse-talk-main\learniverse-talk-main"

# install dependencies
npm install

# start dev server
=======
# Welcome to your Lovable project

## Project info

**URL**: https://lovable.dev/projects/5a7cf82c-cf08-4246-9a73-96bc8d231281

## How can I edit this code?

There are several ways of editing your application.

**Use Lovable**

Simply visit the [Lovable Project](https://lovable.dev/projects/5a7cf82c-cf08-4246-9a73-96bc8d231281) and start prompting.

Changes made via Lovable will be committed automatically to this repo.

**Use your preferred IDE**

If you want to work locally using your own IDE, you can clone this repo and push changes. Pushed changes will also be reflected in Lovable.

The only requirement is having Node.js & npm installed - [install with nvm](https://github.com/nvm-sh/nvm#installing-and-updating)

Follow these steps:

```sh
# Step 1: Clone the repository using the project's Git URL.
git clone <YOUR_GIT_URL>

# Step 2: Navigate to the project directory.
cd <YOUR_PROJECT_NAME>

# Step 3: Install the necessary dependencies.
npm i

# Step 4: Start the development server with auto-reloading and an instant preview.
>>>>>>> 75787e558a22d02d86ba092c5fe844950f261d8d
npm run dev
```

**Edit a file directly in GitHub**

- Navigate to the desired file(s).
- Click the "Edit" button (pencil icon) at the top right of the file view.
- Make your changes and commit the changes.

**Use GitHub Codespaces**

- Navigate to the main page of your repository.
- Click on the "Code" button (green button) near the top right.
- Select the "Codespaces" tab.
- Click on "New codespace" to launch a new Codespace environment.
- Edit files directly within the Codespace and commit and push your changes once you're done.

## What technologies are used for this project?

This project is built with:

- Vite
- TypeScript
- React
- shadcn-ui
- Tailwind CSS

<<<<<<< HEAD
## Available npm scripts

- `npm run dev` — start Vite dev server
- `npm run build` — build for production
- `npm run build:dev` — build in development mode
- `npm run preview` — preview the production build locally
- `npm run lint` — run ESLint

## Environment

Create a `.env` file in the project root for any environment variables. Do NOT commit secrets. The repository already includes a `.gitignore` that excludes `.env` and `node_modules`.

## Build & Deploy

Build the production bundle with:

```powershell
npm run build
```

You can deploy the `dist` output to any static host (Vercel, Netlify, GitHub Pages, Cloudflare Pages, etc.). If you use Vercel, connect the repo and Vercel will use the build command above to publish.

## Contributing

1. Fork or clone the repository.
2. Create a branch for your feature/fix: `git checkout -b feat/my-feature`
3. Make changes and run the app locally.
4. Commit and push, then open a Pull Request.

## Adding this repo to GitHub (if needed)

If you haven't created a remote repository yet, you can create one using GitHub CLI and push the current project:

```powershell
# login once (follow prompts)
gh auth login

# create repo and push (replace <REPO> and choose --public or --private)
gh repo create edutalksorg/<REPO> --public --source=. --remote=origin --push
```

Or create a repo via the GitHub web UI and run locally:

```powershell
git init
git branch -M main
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/edutalksorg/<REPO>.git
git push -u origin main
```

## Notes

- The project already includes Tailwind and TypeScript configuration.
- Double-check `package.json` scripts and `devDependencies` if you plan to upgrade dependencies.

---

If you'd like, I can:

- update this `README.md` with a custom project description (project name, author, license),
- add a `LICENSE` file (MIT by default), or
- create a small CONTRIBUTING.md or project-specific run/debug tips.

Tell me which of the above you'd like and I'll apply it.
=======
## How can I deploy this project?

Simply open [Lovable](https://lovable.dev/projects/5a7cf82c-cf08-4246-9a73-96bc8d231281) and click on Share -> Publish.

## Can I connect a custom domain to my Lovable project?

Yes, you can!

To connect a domain, navigate to Project > Settings > Domains and click Connect Domain.

Read more here: [Setting up a custom domain](https://docs.lovable.dev/features/custom-domain#custom-domain)
>>>>>>> 75787e558a22d02d86ba092c5fe844950f261d8d
